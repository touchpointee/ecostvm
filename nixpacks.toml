# Baileys requires ffmpeg (media processing) and libvips (sharp / birthday card rendering).
# No Chromium or Puppeteer libraries are needed.
[phases.setup]
aptPkgs = [
  "ffmpeg",
  "libvips-dev"
]

[phases.build]
cmds = ["npm run build"]
